import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  icon: {
    color: 'blue',
  },

  error: {
    padding: '0 5px',
    fontSize: '10px',
    color: 'red',
  },
}));
