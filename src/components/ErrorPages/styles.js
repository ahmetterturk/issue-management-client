import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  errorImage: {
    display: 'block',
    width: '70%',
  },
  notFoundBtn: {
    display: 'inline-block',
    color: '#3489eb',
    marginLeft: '5px',
  },
}));
